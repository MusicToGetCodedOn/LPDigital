version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: LPDigital-backend
    restart: always
    ports:
      - "5000:5000"
    # Liest die Variablen direkt aus der .env-Datei im Hauptverzeichnis
    env_file:
      - backend/.env
    volumes:
      - db-data:/usr/src/app/prisma/data
      - ./backend/public/documents:/usr/src/app/public/documents

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: LPDigital-frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  db-data: